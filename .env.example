# Production: aXet SPA App injects window.AXET_CONFIG.flowsBaseUrl at runtime.
# This is only the local/dev fallback used when AXET_CONFIG is not available.
VITE_API_BASE_URL="/references-api/"

# Optional local-only proxy to a FLOWS/Node-RED instance.
VITE_DEV_PROXY_TARGET="http://localhost:1880"
